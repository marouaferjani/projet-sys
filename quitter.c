#include <stdlib.h>
#include <stdio.h>

  int main()
  {
    char choice;
    printf("taper pour Q pour quitter");
    choice=getchar();
    if(choice=='Q')
    {system("exit");
    }else
    {
     printf("Forget Code Retains");
}

    return 0;
  }
